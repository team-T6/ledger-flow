# 인터페이스 정의서

> 상태: v1 — 거래 표 스키마·단계 결과 보고·실행 로그·실행 파라미터 확정. 남은 검토는 "다음 단계" 참고 (PDF 리포트 목차).
> 상위 문서: [PRD](PRD.md) — 열린 질문이 아래 미정·검토 항목과 대응.

## 파이프라인 인터페이스 표

주제: 여러 카드의 결제 내역을 모아 월 결산 산출물(엑셀 장부 + PDF 리포트) 만들기

> 각 단계가 "하는 것"(내부 처리)은 [PRD](PRD.md) §3과 각 단계 문서(`.claude/agents/`) 몫 — 이 표는 단계 사이의 계약(무엇을·어떤 모양으로·어디에·안 될 땐 어떻게)만 다룬다.

| 단계 | 받는 것 | 내놓는 것 | 모양 | 두는 자리 | 못 할 때 |
|---|---|---|---|---|---|
| 수집 | Google Drive의 카드사 엑셀 + Google Drive·Photos의 OCR 대상 이미지 (영수증 사진·결제 문자 캡처 둘 다) + 웹 화면에서 직접 올린 파일(`uploads/inbox/`) | 표준 형식의 거래 내역 표 | CSV — 아래 "거래 표 스키마" (확정 v1) | `collect/` | 원본이 없으면 "수집 대상 없음" 표시. OCR 인식 실패 건은 버리지 않고 오류 표시로 남김 |
| 가공 | 거래 내역 표 | 가맹점·카테고리가 붙은 거래 표 | **CSV** — 거래 표 스키마 동일, 결제처 정규화·카테고리 채움 (체계는 [categories.md](categories.md) 확정) | `refine/` | 가맹점 식별·분류 불가 건은 "확인 필요" 표시하고 버리지 않음 |
| 검증 1 | 가공된 거래 표 (검증 2와 같은 것을 동시에) | 항목별 통과·반려 결과 (분류 관점) | **CSV** — 입력 행 뒤에 판정 열 `verify1_result`(`통과`/`반려`)·이유 열 `verify1_reason`(반려 사유 한 줄, 통과면 빈칸)을 덧붙인 모양 (확정). 행을 지우거나 순서를 바꾸지 않고, 행 대조 키는 `transaction_id` | `verify1/` | 검증할 항목이 없으면 "대상 없음" |
| 검증 2 | 가공된 거래 표 (검증 1과 같은 것을 동시에) | 항목별 통과·반려 결과 (기간·금액 관점) | **CSV** — 검증 1과 같은 모양(확정): 판정 열 `verify2_result`·이유 열 `verify2_reason` | `verify2/` | 검증할 항목이 없으면 "대상 없음" |
| 통합 | 가공된 거래 표(CSV) + 두 검증 결과(CSV) + 과거 월 집계(`archive/<월>/summary.json`, 있으면 — 전월 대비·월별 추이용, 읽기 전용) | 엑셀 회계장부 + PDF 결산 리포트 (한 묶음) | 아래 "산출물 양식" — 장부는 거래 표 스키마, PDF 목차는 제안 검토 중. **중간 산출물(CSV)을 이 단계에서 바이너리(.xlsx·.pdf)로 변환하는 유일한 단계** | `merge/` | 앞 결과가 비거나 반려 미해결이면 그 자리에 "미완" 표시 + 사유 기재 |
| 지휘 | 각 단계의 결과 보고 (아래 "단계 결과 보고" 규격) | 최종 산출물 전달 + 실행 기록 | 아래 "실행 로그 형식" | `logs/` | `failed`면 동일 입력으로 1회 재호출 후 실패 위치 기준 처리 (사용량 한도 초과 실패는 재시도 생략 — 리셋 전엔 같은 결과) — 수집·가공은 이후 중단, 검증 편측은 그 자리 "미완"으로 진행, 검증 양측은 중단, 통합은 요약만 (상세는 [.claude/agents/orchestrator.md](../.claude/agents/orchestrator.md) "판단 규칙"). 수집이 `empty`면 이후 단계 호출 생략. 검증 반려 건은 재시도 없이 통합의 "확인 필요" 목록으로 싣는다 (반려 루프 없음) |

> 두는 자리 확정: 단계 영어명 = repo 최상위의 "칸 폴더"명 (`collect/` `refine/` `verify1/` `verify2/` `merge/` — 담당자별 1칸). 각 칸 폴더에는 그 단계에 필요한 것이 전부 모인다: 약속(`README.md`) · 받는 재료 견본(`input-sample.md`) · 산출물 모양 견본(`stub.md`) · 진짜 산출물(`result.*`) · 산출물 생성에 코드가 필요하면 그 코드까지. 칸 폴더는 옮기지 않고, 기존 파일은 지우지 않는다.
>
> 커밋 정책: 견본(`input-sample.md`·`stub.md`)은 `sample_data/` 기반 가짜 데이터로만 작성해 커밋한다. **실데이터로 실행한 `result.*`는 커밋 금지** — .gitignore가 `*.xlsx`/`*.csv`/`*.pdf`를 차단하고 있어(`sample_data/` 제외) 기본적으로 걸러지며, 가짜 데이터 기반 결과를 공유하고 싶을 때만 팀 확인 후 예외로 올린다. 지휘의 실행 로그는 종전대로 `logs/`에 두고 커밋 차단(`logs/.gitkeep`만 커밋).

## 단계별 판단할 것

각 단계의 핵심 판단과, 애매할 때 참조할 곳. 상세 판단 규칙은 각 단계 문서(`.claude/agents/<단계>.md`)에서 구체화한다.

| 단계 | 여기서 판단할 것 | 애매하면 무엇을 더 보나 |
|---|---|---|
| 수집 | OCR로 읽은 값이 거래 내역으로 맞는지 (금액·날짜 인식 오류 여부) | 원본 이미지·카드사 엑셀 원본 |
| 가공 | PG사 이름 뒤의 실제 가맹점이 어디인지, 어느 카테고리인지 | 원본 거래 내역의 부가 정보, 카테고리 체계 ([categories.md](categories.md) 확정) |
| 검증 1 | 부여된 카테고리가 체계에 맞는지 | 카테고리 체계 ([categories.md](categories.md) 확정) |
| 검증 2 | 대상 월 데이터가 맞는지, 해외결제 환산 금액이 맞는지, `금액`이 기입돼 있고 부호 규칙(지출 음수·수익 양수)에 맞는지 | 카드사 원본 내역 · 기준 환율 = 현금은 결제일 환율, 카드는 카드사 부여 환율 (추후 수정 상정) |
| 통합 | 어떤 구성으로 실어야 읽히는지 (장부 컬럼·리포트 목차) | 아래 "산출물 양식" 초안 |
| 지휘 | 부분 실패 시 파이프라인을 중단할지 남은 단계를 계속 갈지 | 실행 로그·오류 내용 |

## 거래 표 스키마 (확정 v1)

수집이 만들어 통합까지 그대로 흐르는 공통 컬럼. 바꿀 땐 이 표만 고치고 각 단계는 여길 따른다.

| 컬럼 | 설명 | 채우는 단계 |
|---|---|---|
| `transaction_id` | 거래 고유 번호 (`tx_{YYMMDD}_{순번}`) — 행 식별과 검증 결과 대조의 기준 키. 할부는 회차가 지나도 같은 값 유지 | 수집 |
| 날짜 | 결제일 | 수집 |
| 금액 | 부호로 지출/수익 구분 — 지출은 음수, 수익은 양수. 비었거나 숫자로 해석 불가면 오류 (검증 2 확인) | 수집 |
| 결제처 | 실제 가맹점명 — PG사 표기는 가공이 정규화 | 수집 → 가공 |
| 카테고리 | [categories.md](categories.md) 체계 (확정) — 결제처가 애매하면 구매항목을 참고 | 가공 |
| 비고 | 부가 정보 (원문 표기·메모) | 수집·가공 |
| 결제수단 | 카드·현금 등 | 수집 |
| 결제구분 | `개인결제` / `법인결제` — 결제수단 명의 기준 | 수집 |
| 원거래통화 | 해외결제면 원거래 통화 코드(예: `USD`), 국내 결제는 빈칸 — **해외결제 여부는 이 컬럼이 채워져 있는지로 판별한다** (별도 여부 컬럼 없음) | 수집 |
| 원거래금액 | 해외결제면 원거래 통화 기준 금액, 국내 결제는 빈칸 | 수집 |
| `source_type` | 원천 구분 — `card_excel`(카드사 엑셀)·`receipt`(영수증)·`manual`(직접 입력) | 수집 |
| `collect_status` | 수집 품질 — `확인됨`·`대기`·`확인 필요` | 수집 |
| 구매항목 | 영수증에서 추출한 구매 물품, 콤마로 구분 (마트·편의점처럼 결제처만으론 뭘 샀는지 알기 어려운 경우 가공의 카테고리 판단을 돕는 용도) — 영수증이 없거나 확인 전이면 빈칸. 맨 끝 컬럼 | 수집 |

- 해외결제 환율: 현금 결제는 결제일 환율, 카드 결제는 카드사 부여 환율 기준 (추후 수정 상정). 검증 2의 환산 검증은 재계산값 대비 **오차 ±1% 이내면 통과** (초기값 — 운영하며 조정)
- 수집 내부 필드였던 `transaction_id`·`source_type`·`collect_status`는 공식 컬럼으로 편입했다 — 가공 이후 단계도 그대로 보존해 흘리고, 검증·통합은 `transaction_id`를 행 대조 키로 쓴다

## 실행 파라미터 (확정)

- **대상 월** (`YYYY-MM`) — 결산 실행 1회의 범위. 지휘가 사용자 요청에서 확정해 **각 단계 호출 시 파라미터로 전달**하고 실행 로그에 기록한다
- 수집은 대상 월 위주로 모으되 걸러내는 책임은 지지 않는다 — 범위 밖 데이터의 반려는 검증 2가 담당한다 (필터 책임을 한 곳에 유지)

## 산출물 양식 (초안)

- **엑셀 회계장부**: 거래 표 스키마와 동일 컬럼 (초안 — 추후 확정)
- **PDF 결산 리포트 목차 (제안 — 팀 검토, 2026-08-26 개정)**:
	1. 결산 개요 — 대상 월·대상 카드(결제수단×결제구분), 총지출/총수익/순액. 앞 단계 결과가 비어 "미완"인 자리가 있으면 여기에 함께 고지. 리포트 제목에 대상 연월을 표기 (예: "2026년 8월 결산 리포트")
	2. 월별 비교·추이 — 전월 대비 총지출/총수익/순액 증감 + 같은 해 보관된 월들의 추이 그래프(월 4개 이하는 막대, 5개 이상이면 지출·수익 꺾은선). 데이터 원천은 지휘의 월별 보관(`archive/<월>/summary.json`) — 보관 데이터가 없으면 "비교 대상 없음" 표기 (통합이 `archive/`를 읽기 전용으로 접근 — **지휘 담당 합의 필요**)
	3. 카테고리별 지출 — 금액·비중 (파이 그래프 + 표)
	4. 결제수단별 합계 — 금액·비중 (가로 막대 + 표)
	5. 결제구분별 분석 — 개인/법인 합계·비중(100% 누적 가로 막대 + 표) + 구분별 카테고리 세부 집계 (개인·법인은 카테고리 체계가 이원화되어 있어 각자 자기 체계로 집계 — [categories.md](categories.md))
	6. 주요 지출 Top 10 — 지출 행만 대상 (가로 막대 + 표)
	7. 해외결제 명세 — 환산 내역
	8. 확인 필요 항목 — 검증 반려·미완·오류. 컬럼: `transaction_id`·날짜·결제처·유형·사유 (장부 대조 키 포함)

## 단계 결과 보고

모든 단계는 산출물과 별개로 **같은 형식의 결과 보고**(envelope pattern)를 지휘에게 돌려준다. 지휘는 이 보고만 읽고 다음 호출·중단·오류 처리를 판단하며, 산출물 내용은 열어보지 않는다.

```json
{
  "stage": "collect",
  "status": "ok",
  "output": "collect/result.csv",
  "counts": { "total": 123, "ok": 120, "flagged": 3 },
  "flags": [
    { "row": 12, "type": "확인 필요", "reason": "가맹점 식별 불가" }
  ],
  "message": ""
}
```

- 필드 설명 (1뎁스):
	- `stage` — 보고 주체 (단계 영어명: `collect`·`refine`·`verify1`·`verify2`·`merge`)
	- `status` — 단계 전체 상태 (아래 4개 어휘 중 하나)
	- `output` — 산출물 파일 경로. 산출물 형식은 단계마다 다르며(인터페이스 표 "모양" 열) 이 필드는 경로만 담는다. 여러 개면 배열(예: 통합의 엑셀 + PDF), `empty`·`failed`면 빈 값
	- `counts` — 처리 건수 요약: `total` 입력 전체 · `ok` 정상 · `flagged` 문제 건. **단위는 전 단계 공통 "거래 행"** — 수집은 추출한 행, 검증은 검사한 행, 통합은 장부에 실은 행 (파일·이미지 개수는 `message`에). 단계 간 `total` 대조가 유실 감지의 근거
	- `flags` — 행 단위 문제 목록 (`row` 행 번호 · `type` 문제 유형 · `reason` 사유 한 줄)
	- `message` — 사람이 읽는 자유 메모 (없으면 빈 값). **`failed`일 때는 실패 사유를 반드시 적는다** — 빈 `message`의 `failed`는 규격 위반이며, 지휘는 요약에 "사유 미기재 실패"로 명시한다
- `status` 어휘 (4개 고정):
	- `ok` — 정상 산출
	- `empty` — 처리할 대상 없음 ("수집 대상 없음"·"대상 없음"이 여기 대응)
	- `partial` — 산출했지만 flagged 건 있음 ("확인 필요"·"미완"·반려 건 포함)
	- `failed` — 단계 실패, 산출물 없음 (지휘는 중단·오류 보고)
- `flags[].type` 어휘: `확인 필요`(가공) · `반려`(검증) · `오류`(수집 OCR 실패 등) · `미완`(통합)
- 인터페이스 표의 "못 할 때" 문구는 사람용 표현이고, 이 보고가 그 기계 규격판이다. 보고는 지휘가 실행 로그에 그대로 기록한다.
- **전달 방식 (확정)**: 보고는 단계가 파일로 남기는 것이 아니라 **단계 호출의 응답(반환값)으로 지휘에게 직접 전달**한다. 호출 계층에서 이 규격의 JSON 스키마를 강제하고(구조화 출력), 형식 위반은 호출 실패로 처리한다. 지휘는 받은 보고 원문을 실행 로그 디렉터리에 `report-<단계>.json`으로 아카이브한다 (아래 "실행 로그 형식"). 단계가 보고를 자기 칸에 별도 파일로 남기지 않는다 — 기존 `collect/result-report.json` 방식은 이 확정으로 대체한다 (collect의 화면은 데이터 준비 도구로 두고, 파이프라인의 수집 "실행"은 준비된 데이터를 읽어 보고를 반환하는 호출로 분리).

## 실행 로그 형식

- 실행 1회당 run 디렉터리 하나: `logs/run_YYYYMMDD_HHMMSS/` (쓰는 주체는 지휘 하나)
	- `run.log` — 진행 로그. 줄 형식: `시각 | 단계 | 상태(시작/완료/실패) | 처리 건수 | 메모` — 웹 실행 경로의 중간 확인 시에는 상태 `확인 대기`/`확인 반영` 줄이 추가로 남는다 (아래 "다음 단계" 2026-08-26 중간 확인 확정 로그)
	- `report-<단계>.json` — 각 단계가 반환한 결과 보고 원문 아카이브 (위 "단계 결과 보고" 전달 방식)

## 세는 법 메모

- 산출물 개수가 아니라 **갈라져 나가는 목적지 수**로 단계를 센다. 엑셀 장부 + PDF 리포트는 한 묶음으로 같은 곳에 전달되므로 통합은 한 단계.
- 검증 1·2는 같은 입력을 동시에 받는 병렬 구조 — 두 검증 리포트는 통합 전에 merge.
- 이후 웹 확장 등으로 산출물이 서로 다른 소비자에게 갈라지면 그때 단계를 쪼개고 분담표를 함께 고친다.

## 다음 단계

미정·검토 항목이 확정되는 대로 이 문서의 해당 표기를 갱신한다. 남은 것: PDF 리포트 목차 (제안 검토 중).

- 확정됨(2026-08-26): **중간 확인 (사용자 입력) — 웹 실행 경로 한정** — 수집·가공 완료 직후, 그리고 검증 1·2가 모두 끝난 직후(통합 직전)에 그 시점의 flags 건이 있으면 지휘가 파이프라인을 일시정지하고, 해당 행 데이터를 웹 화면으로 보내 사용자 입력(행 단위 수정 또는 그대로 두기)을 받는다. 수정의 효력은 단계에 따라 다르다: **수집·가공 시점 수정**은 그 칸 `result.csv`에 반영되어(비고에 `[사용자 확인]` 이력, 수집은 `collect_status`를 `확인됨`으로) 이후 단계가 재처리하고, **검증 시점 수정**은 `refine/result.csv` 값을 갱신하고 반려했던 검증 `result.csv`의 판정을 `통과`·사유 "사용자 확인"으로 갱신해 통합이 장부에 싣는다. 수정된 행은 그 단계 보고의 flags에서 빠지고 counts가 갱신되며, 그대로 둔 행은 기존 흐름(확인 필요 목록)을 따른다. 응답 대기 상한 10분 — 초과하면 전부 "그대로 두기"로 진행. run.log 상태 어휘에 `확인 대기`/`확인 반영`이 추가되고, CLI 실행 경로는 종전대로 정지 없이 진행한다. 단계 결과 보고 규격·거래 표 스키마는 변경 없음 (지휘·웹 소관 확장이나, **지휘가 각 칸의 `result.csv`를 사용자 확인 이력과 함께 갱신하는 동작은 담당자 공유 필요**).

- 확정됨(2026-08-26): **웹 실제 모드 산출물 월별 보관 + 진행 이벤트** — 결산이 정상 종료(통합 ok/partial)되면 파이프라인이 실행 경로와 무관하게(웹 서버·CLI·직접 호출) 최종 산출물(엑셀 장부·PDF 리포트·결과 요약)과 집계 수치(`summary.json`)를 `archive/<YYYY-MM>/`에 복사해 보관하고, 화면의 월 선택이 보관된 월을 여는 입구(전월 대비 포함)가 된다. 진행 관찰용으로 파이프라인이 `state: "진행"` 이벤트를 웹 서버에만 흘린다(가공·검증은 산출 행 수, 수집(웹 업로드)은 처리 파일 수 기준 — CLI 폴백은 스트리밍 출력이 가능할 때만) — run.log 실행 로그 형식은 변경 없음(시작/완료/실패만 기록). 단계 결과 보고·거래 표 스키마·단계 간 전달 등 인터페이스 표의 다른 행은 변경 없음 (지휘·웹 소관 확장, `archive/`는 실거래 정보라 커밋 차단).

- 확정됨(2026-08-25): **수집 입력 원천에 웹 업로드 추가** — 사용자 페이지(`web/index.html` 실제 모드)에서 올린 파일이 `uploads/inbox/`에 놓이고, 지휘가 수집을 부를 때 이 폴더를 입력으로 넘긴다(`collect/collect_uploads.py`). 산출물 스키마·보고 규격은 기존 수집과 동일 — 인터페이스 표의 다른 행은 변경 없음.

- 확정됨: **단계 결과 보고 형식**과 **실행 로그 형식**을 위 규격대로 확정한다.

- 확정됨(2026-08-19): **거래 표 스키마 v1** — ① 지출·수익 두 컬럼을 부호 기준 단일 `금액` 컬럼으로 통합(지출 음수·수익 양수) ② `결제자`를 `결제구분`(`개인결제`/`법인결제`)으로 대체 — 수집부터 이 이름·값으로 산출 ③ 해외결제 식별 컬럼 `원거래통화`·`원거래금액` 추가(수집이 채움, 환산 검증 오차 허용 ±1% 초기값) ④ 수집 내부 필드였던 `transaction_id`·`source_type`·`collect_status`를 공식 컬럼으로 편입. ①·②는 2026-08-12 가공·검증 2 구두 합의를 팀 확정으로 공식 반영한 것.

- 확정됨(2026-08-19): **카테고리 체계 확정** — [categories.md](categories.md)를 제안에서 확정으로 전환. 변경은 식대 중복 2줄 병합 하나 (그 외 명칭·구성 유지).

- 확정됨(2026-08-19): **실행 파라미터(대상 월)** — 위 "실행 파라미터" 절 규격대로. 지휘가 확정해 각 단계에 전달, 범위 밖 반려는 검증 2 담당.

- 확정됨(2026-08-19): **통합(merge)에 `Bash` 도구 부여** — CSV→xlsx·pdf 변환의 실행 수단. 아래 2026-08-12 결정의 열린 과제를 해소. 수집은 현 범위(CSV 처리·이미지 Read)에서 불필요해 보류.

- 확정됨(2026-08-19): **지휘의 `failed` 처리 정책** — 전 단계 공통 재시도 1회 · 실패 위치 기준 중단(인터페이스 표 지휘 행) · 수집 `empty` 특례 · `failed` 보고의 `message` 필수. 판단 규칙 상세는 [.claude/agents/orchestrator.md](../.claude/agents/orchestrator.md) "판단 규칙".

- 확정됨(2026-08-19): **결과 보고 전달 방식** — 단계 호출의 응답(반환값)으로 직접 전달 + 지휘가 run 디렉터리에 아카이브 ("단계 결과 보고"·"실행 로그 형식" 참고). 칸별 보고 파일(`collect/result-report.json`)은 폐기 — collect의 명세서·지시서·화면/서버 코드에 반영 완료 (화면의 보고 기능은 미리보기·복사만 남김).

- 확정됨(2026-08-19): **검증 결과 CSV 세부 컬럼** — 검증 1 견본 방식으로 통일: 입력 행 뒤에 `verify{n}_result`(`통과`/`반려`)·`verify{n}_reason` 두 열을 덧붙이고, 행 삭제·순서 변경 없이 `transaction_id`로 대조한다 (인터페이스 표 검증 1·2 행).

- 확정됨(2026-08-12 재검토): **수집→가공→검증 1→검증 2 사이의 중간 전달 형식은 CSV로 통일한다.** 이전에 "가공·검증 1은 엑셀(.xlsx)"로 합의했던 것을 이 결정으로 대체한다.
  - 이유: `collect`·`merge`·`orchestrator` 등 상당수 에이전트가 `Bash` 없이 `Read`/`Write`만으로 동작한다(`.claude/agents/*.md`의 `tools:` 참고) — 코드 실행 없이는 `.xlsx` 같은 바이너리 형식을 직접 열거나 만들 수 없고, CSV는 텍스트라 이 도구만으로도 다룰 수 있다
  - 예외: **최종 산출물(통합의 엑셀 회계장부 + PDF 결산 리포트)만 그대로 바이너리로 유지**한다 — 사람이 최종 결과물로 받는 문서이므로 CSV로 대체할 수 없다
  - ~~열린 과제: 통합(`merge`)이 CSV 입력을 실제 `.xlsx`·`.pdf`로 변환하려면 코드 실행 수단(예: `Bash` 도구 추가)이 필요하다~~ — 해소: 통합에 `Bash` 부여 확정 (위 확정 로그)
