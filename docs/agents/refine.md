# 가공 에이전트 설계

> 담당: 조영진 · 상태: 초안
> 입출력의 정본은 [인터페이스 정의서](../interface-spec.md) — 여기에는 요약과 링크만 두고, 상세 스키마를 재정의하지 않는다.

## 1. 역할

(이 단계가 파이프라인에서 맡는 일 한 줄)

## 2. 입출력 요약

- 받는 것: (한 줄 요약 → 상세는 인터페이스 정의서 해당 단계)
- 내놓는 것: (한 줄 요약 → 상동)

## 3. 내부 처리 절차

(단계 안에서 도는 절차를 순서대로. 절차가 여럿인 게 정상)

1.
2.

## 4. 판단 규칙

(인터페이스 정의서 "단계별 판단할 것"의 내 단계를 구체화 — 무엇을 어떤 기준으로 판단하나, AI 판단이 필요한 곳과 일반 코드로 처리할 곳 구분)

- 판단할 것:
- 판단 기준:
- 애매할 때 참조:
- 핵심 지시 (이 에이전트에게 시키는 말의 요지):

## 5. 오류·예외 처리

(입력이 깨졌을 때, 판단이 불가능할 때 어떻게 하나 — 실패를 조용히 삼키지 않는다)

## 6. 사용 도구·의존성

(라이브러리·API·모델)

## 7. 테스트 방법

(샘플 입력 → 기대 출력. sample_data/ 기준, 실데이터 사용 금지)

## 8. 막힌 점

(문서 곳곳의 미정 표시와 별개로, 단계 간 계약 변경 요청·의견 갈림처럼 팀에 알려야 풀리는 블로커를 적는 자리. 없으면 "지금은 없음"이라고 적는다)
